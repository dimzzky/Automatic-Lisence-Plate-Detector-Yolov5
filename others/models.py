# import torch

# def load_model(model = torch.hub.load('ultralytics/yolov5', 'custom', path='models/yolov5s_custom.pt', force_reload=True)):
#     device = torch.device('cpu')
#     model = model.fit(device)
#     model.eval()

#     return model
